name: Test
on: [push, pull_request]
jobs:
  run:
    strategy:
      fail-fast: false
      matrix:
        ruby-version:
          - "3.2"
          - "3.3"
          - "3.4"
          - "4.0"
        include:
          - ruby-version: "ruby-head"
            continue-on-error: true
    name: ${{ matrix.ruby-version }}
    runs-on: ubuntu-latest
    env:
      TZ: "Asia/Tokyo"
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Install mandoc
        run: |
          sudo apt-get update
          sudo apt-get install -y mandoc
      - uses: ruby/setup-ruby@c984c1a20bb35a1cbda04477c816cea024418be9 # v1.294.0
        with:
          ruby-version: ${{ matrix.ruby-version }}
          bundler-cache: true
      - name: Tests
        run: |
          bundle exec rake test
          bundle exec rake build
          gem install pkg/rdtool-*.gem
        continue-on-error: ${{ matrix.continue-on-error || false }}
      - name: Lint rd2man output
        run: |
          ruby -Ilib bin/rd2 -r rd/rd2man-lib README.rd > rdtool.1
          mandoc -T lint rdtool.1
        continue-on-error: ${{ matrix.continue-on-error || false }}
# MRI:
#   name: ${{ matrix.os }} ruby-${{ matrix.ruby }}
#   runs-on: ${{ matrix.os }}
#   strategy:
#     fail-fast: false
#     matrix:
#       os: [ubuntu-latest, macos-11, windows-2022]
#       ruby: [ '3.1', '3.2,', '3.3', '3.4', head]
#       include:
#         - { os: windows-2022 , ruby: mswin }
#   steps:
#     - uses: actions/checkout@v2
#     - name: Set up Ruby ${{ matrix.ruby }}
#       uses: ruby/setup-ruby@v1
#       with:
#         ruby-version: ${{ matrix.ruby }}
#     - name: Build and Install
#       run: |
#         gem install --no-document bundler
#         gem build numru-narray.gemspec
#         gem install numru-narray-*.gem
#         bundle install
#     - name: Tests
#       run: rake test
#   ruby-head-debug:
#     name: Build on ruby-head-debug
#     runs-on: ubuntu-latest
#     continue-on-error: true
#     strategy:
#       fail-fast: false
#       matrix:
#         ruby: [ 'debug' ]
#     steps:
#       - uses: actions/checkout@v2
#       - name: Set up Ruby ${{ matrix.ruby }}
#         uses: ruby/setup-ruby@v1
#         with:
#           ruby-version: ${{ matrix.ruby }}
#       - name: Build and Install
#         run: |
#           gem install --no-document bundler
#           gem build numru-narray.gemspec
#           gem install numru-narray-*.gem
#           bundle install
#       - name: Tests
#         run: rake test
